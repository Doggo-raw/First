import React from 'react';
import styles from './ModalOpeningBtn.module.css';

const ModalOpeningBtn = () => {
    return (
        <button className={styles.open}>Create post</button>
    );
};

export default ModalOpeningBtn;